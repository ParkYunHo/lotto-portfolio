spring:
  batch:
    job:
      enabled: true
      name: ${job.name}
  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 3000
            read-timeout: 3000
